type Mutation {
  triggerWorkflowRun(
    workflow_id: uuid!
  ): TriggerWorkflowRunOutput

  approveStep(
    step_run_id: uuid!
  ): ApproveStepOutput
}

type TriggerWorkflowRunOutput {
  workflow_run_id: uuid!
  status: String!
}

type ApproveStepOutput {
  workflow_run_id: uuid!
  step_run_id: uuid!
  status: String!
}
